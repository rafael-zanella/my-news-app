import { Meta } from '@storybook/react'
import { LogoLogin } from './LogoLogin'

export default {
  title: 'Components/LogoLogin',
  component: LogoLogin
} as Meta

export const Normal = () => <LogoLogin />
